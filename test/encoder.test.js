'use strict';

// Modules
const tap = require('tap');
const Encoder = require('./../index').Encoder;
const EncoderProperty = require('./../index').EncoderProperty;

// Objects
const encoder = new Encoder();

tap.test('test the payload is divisible by 2', (t) => {
    t.end();
});

